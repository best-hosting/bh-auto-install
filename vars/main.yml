---
# vars file for bh-auto-install

# Internal os_install .
_host_conf: {}

images_checksum_dir: "{{ playbook_dir + '/image_checksums' }}"

_pxe_os: {}
_os_iso: {}

file_does_not_exist: "{{ '/' + 65534 | random(seed=_host_conf.name) | hash('sha1') }}"
